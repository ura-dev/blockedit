# CLAUDE.md

リポジトリ横断の取り決めだけを置く。領域に閉じたものは `.claude/rules/` にあり、
該当ファイルを触るときだけ読み込まれる。ツールの使い方は [README.md](README.md)、
計測ハーネスは [bench/README.md](bench/README.md)。

## このリポジトリを編集対象にしない

blockedit は**他のリポジトリを編集する道具**で、置き場所と対象は別物。ここで
引数なしに `blockedit.py` を起動すると、カレントのgitルート = このリポジトリ自身が
対象になる。それは事故なので `task._guard_self` が起動を拒否する。動作を見たいときは
`--root` で外のリポジトリを渡す(`--select-only` なら0トークン)。

この門を「テストのため」に外さない。外して通すくらいなら対象を用意する。

## 行末はLF

`.gitattributes` が `* text=auto eol=lf` を敷いている。**「変更範囲外はバイト同一」を
名乗る道具なので、自分のソースに改行コードの揺れを持ち込まない。**Windows上で Python の
`write_text` を使うと CRLF が混ざる — 書き戻すときは `write_bytes` を使うか、
`git diff --stat` で LF 警告が出ていないか見る。

## Git

- コミットメッセージにトレーラを付けない(`Co-Authored-By:` を含む)。公開名義は `ura-dev`
- `--force` push と `git reset --hard` はしない。壊す操作が要るならブランチを切る
- コミットしてよいかを毎回確認しなくてよい

## やらないこと

- 頼まれていないリファクタ
- 設計判断の代行。選択肢を出して止まる
- 最終メッセージを長くしない。何がどうなったかを先に書く

## ルールの置き場所

パスに閉じたルールは CLAUDE.md ではなく `.claude/rules/` に置く。frontmatter の
`paths:`(glob)で対象を限定でき、該当ファイルを読み書きするときだけ文脈に入る。
CLAUDE.md は毎回全部読まれるので、ここには横断的なものだけを残す。
